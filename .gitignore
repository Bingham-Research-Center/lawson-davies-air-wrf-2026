## LaTeX build artifacts
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.lof
*.log
*.lot
*.out
*.run.xml
*.spl
*.synctex.gz
*.toc

## NOTE: the rendered main.pdf is intentionally TRACKED (see README) — not ignored.
## Auto-generated EPS->PDF logo conversions from the build ARE ignored:
*-eps-converted-to.pdf

## Editor / OS cruft
*.swp
.DS_Store

## Personal, per-machine Claude settings (shared .claude/settings.json stays tracked)
.claude/settings.local.json

## Obs data — bulk timeseries kept LOCAL, never committed (small result/score CSVs stay tracked)
*.parquet

## CHPC provenance run logs — large raw dumps kept out of the repo (see verification/provenance/)
*.log.txt

## CHPC provenance inventory tarballs — evidence archive kept LOCAL, never committed to the public repo
## (regenerate with verification/provenance/chpc_wrf_provenance.sh; see CHPC-WRF-INVENTORY-RESULTS.md)
wrf_provenance_*.tgz

## Python bytecode
__pycache__/
